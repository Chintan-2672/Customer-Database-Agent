# Customer-Database-Agent
In this repository, I have implemented an agent-based workflow using LangGraph, where the agent understands the user's natural-language query, converts it into an SQL query, executes it against the database, and provides the user with the relevant answer.
